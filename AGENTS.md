# AGENTS.md（Codex 入口）

## 目录软链接入口

- 需求文档：[`prds/`](./prds/)
- 业务文档：[`docs/`](./docs/)
- 计划文档[`plans/`](./plans/)
- 会话记录[`sessions/`](./sessions/)
- 后端服务[`backend/`](./backend/)

## Codex 差异项

- 批量变更前先定位最小影响范围。
- 输出需包含：改动文件列表、验证命令结果、未覆盖风险。
- 非必要不做大规模重构；优先最小改动完成需求。
- 迁移旧项目代码时，不要迁移原项目中已注释的代码。

## 代码规范

### 通用规范

- 代码优先保证可读性和可维护性，避免过度抽象。
- 遵循已有项目代码风格，不主动引入新的编码范式。
- 修改代码前先理解上下文，避免无关格式化和大范围重排。
- 优先使用已有工具、组件、公共方法，避免重复实现。
- 删除无效代码，不保留废弃逻辑和无用注释。

### 命名规范

- 变量、函数、方法使用语义化命名，避免单字符或无意义缩写。
- 常量使用全大写 + 下划线命名。
- 布尔类型变量使用 `is`、`has`、`can` 等前缀。
- 文件名遵循项目现有规范，不随意修改已有文件命名。

### JavaScript 规范

- 优先使用 `const`，仅在必要时使用 `let`，禁止使用 `var`。
- 异步逻辑优先使用 `async/await`，避免多层 Promise 链，必要时使用 `try catch finally`。
- 避免深层嵌套，通过提前返回降低复杂度。
- 公共逻辑抽离为独立函数，避免组件或业务文件过大。

### 注释规范

- 注释使用中文描述业务原因，而不是重复代码含义。
- 复杂逻辑、算法、兼容性处理必须添加必要说明。
- 禁止保留已废弃代码的注释块，函数添加必要的简述功能、入参、出参。
- TODO 需要说明原因和后续处理方向。

## Git 提交规范

- 提交信息简洁明确，描述实际变更内容。
- 一个提交尽量只包含一个主题修改。
- 禁止提交调试代码、临时文件和无关格式化修改。
- 禁止自动 Push，用户审查后再手动 Push

## 协作与交互约束

- 代码注释与文档默认使用中文编写。
- 每轮交互结束后（一次用户输入 + AI 完成回复与执行），将该轮对话内容追加写入 `sessions/session_yyyy-MM-dd.md`（同日文件追加，不覆盖）。
- 梳理文档保存到 ./docs/，命名规则 `yyyy-MM-dd_xxx.md`。
- 复杂任务，优先制作实现计划，然后计划逐个实现并同步进度，相关文档保存到 ./plans/ 目录中，命名规则 `yyyy-MM-dd_xxx.md`。
